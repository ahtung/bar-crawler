h2 Route

p
  = "From #{@path.start_address} to #{@path.end_address}"

#map data-start-lat="#{@path.start_lat}" data-start-long="#{@path.start_long}" data-end-lat="#{@path.end_lat}" data-end-long="#{@path.end_long}" style='width: 800px; height: 400px;'

.bars

button Print